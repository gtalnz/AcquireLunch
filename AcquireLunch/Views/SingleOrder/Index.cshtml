@model IEnumerable<AcquireLunch.Models.SingleOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Notes)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Person.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Notes)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Person.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SingleOrderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SingleOrderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SingleOrderId })
        </td>
    </tr>
}

</table>
